# prompts.py

PROMPT_TEMPLATE = """
Твоя задача - преобразовать логическую головоломку с естественного языка в Python-скрипт, использующий библиотеку Z3-solver. Не решай задачу сам, а только напиши код для ее решения.

Вот правила преобразования:
1.  Импортируй все необходимое из `z3`: `from z3 import *`.
2.  Создай булевы переменные для каждого утверждения. Например, `alexey = Bool('alexey')` означает "Алексей виновен".
3.  Создай экземпляр солвера: `s = Solver()`.
4.  Переведи каждое условие задачи в логическое выражение Z3 и добавь его в солвер с помощью `s.add()`:
    - "Если A, то B" переводится как `Implies(A, B)`.
    - "A и B" переводится как `And(A, B)`.
    - "A или B" переводится как `Or(A, B)`.
    - "Не A" переводится как `Not(A)`.
    - "A и B не могут быть вместе" переводится как `Not(And(A, B))` или `Or(Not(A), Not(B))`.
    - "Ровно один из {A, B, C} истинен" переводится как `Sum([If(v, 1, 0) for v in [A, B, C]]) == 1`.
5.  В конце добавь код для проверки выполнимости (`s.check()`) и вывода модели (решения), если оно существует.

Пример задачи: "Есть А и Б. Либо А, либо Б правдивы. Но не оба. Кто правдив?"
Пример кода:
```python
from z3 import *
A = Bool('A')
B = Bool('B')
s = Solver()
# Либо А, либо Б
s.add(Or(A, B))
# Но не оба
s.add(Not(And(A, B)))
if s.check() == sat:
    m = s.model()
    print("Решение найдено:")
    for d in m.decls():
        if is_true(m[d]):
            print(f"{d.name()} правдив.")
else:
    print("Решение не найдено.")
"""